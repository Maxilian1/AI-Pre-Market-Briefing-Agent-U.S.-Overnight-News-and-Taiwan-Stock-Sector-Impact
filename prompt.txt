Read AGENTS.md, README.md, and research_design.md first.

Task:
Implement Phase 6C only: multi-day research panel, baseline controls, and regression diagnostics.

Important:
Do not implement live trading.
Do not generate buy/sell recommendations.
Do not implement portfolio optimization or execution logic.
Do not call LLM APIs.
Do not scrape websites.
Do not change upstream RSS/news classification/mapping/reporting logic.
Do not claim predictive power from fixture or one-day data.

Context:
Phase 6B.1 is complete.
Current event-study diagnostics now aggregate by target/day by default.
Recent validation:
- tests: 125 passed
- input return label rows: 24
- aggregated rows: 9
- same-day same-ticker duplicate keys: 0
- candidate_count_sum: 24
- available aggregated rows: 8
- unavailable aggregated rows: 1
- bucket labels include neutral, potentially_negative, potentially_positive
- no forbidden trading language

Now we need a research panel and baseline-control regression layer.

Research question:
Do overnight U.S. semiconductor / AI news signals provide incremental explanatory power for Taiwan semiconductor / AI supply-chain returns after controlling for U.S. market and sector returns?

Goal:
Create a multi-day target-level panel and run simple regression diagnostics.

Inputs:
- one or more data/processed/return_labels_YYYYMMDD.csv files
- optional normalized market price data containing U.S. controls:
    QQQ
    SOXX
    SMH
    NVDA
    AMD
    TSM
- event study aggregation logic from Phase 6B

Outputs:
- data/processed/research_panel_<label>.csv
- data/processed/baseline_controls_<label>.csv
- data/processed/regression_results_<label>.csv
- data/reports/regression_diagnostics_<label>.md

Definitions:
For Taiwan trading date D, U.S. control returns should use the most recent U.S. trading date strictly before D, based on available price data.
Do not assume calendar D-1 is always a trading day.
Use available trading dates in the normalized price data.

Default dependent variables:
- prev_close_to_open_return
- open_to_close_return
- close_to_close_return
- next_close_to_close_return

Default signal features:
- candidate_count
- positive_count
- negative_count
- neutral_count
- sum_impact_score
- mean_impact_score
- max_abs_impact_score
- mean_combined_confidence
- final_directional_label dummies:
    is_potentially_positive
    is_potentially_negative
    is_neutral

Default baseline controls:
- qqq_return
- soxx_return
- smh_return
- nvda_return
- amd_return
- tsm_adr_return

Implementation requirements:

1. Create src/backtest/research_panel.py

Implement:
- load_return_label_files(paths) -> pandas.DataFrame
- build_target_day_panel(return_labels_df, aggregation_mode="target_day") -> pandas.DataFrame
- add_directional_dummies(panel_df) -> pandas.DataFrame
- validate_panel(panel_df) -> dict

Behavior:
- Reuse aggregate_target_day_signals from Phase 6B.
- Keep only return_data_available=True rows for regression panel by default.
- Preserve unavailable rows in an optional QA output if useful.
- Ensure one row per:
    taiwan_trading_date
    return_target
- Add warning column or notes if duplicate keys are detected.
- Sort by taiwan_trading_date and return_target.

2. Create src/backtest/baseline_controls.py

Implement:
- load_normalized_prices(path) -> pandas.DataFrame
- compute_control_returns(price_df, control_tickers=None) -> pandas.DataFrame
- align_controls_to_taiwan_dates(panel_df, control_returns_df) -> pandas.DataFrame

Rules:
- U.S. controls use previous available U.S. trading date strictly before Taiwan trading date.
- Control tickers:
    QQQ -> qqq_return
    SOXX -> soxx_return
    SMH -> smh_return
    NVDA -> nvda_return
    AMD -> amd_return
    TSM -> tsm_adr_return
- If a control ticker is unavailable, create the column as NaN and include a warning.
- Do not drop rows only because controls are missing.

3. Create src/backtest/regression_diagnostics.py

Use statsmodels if available.

Implement:
- run_ols_diagnostic(df, y_col, x_cols, min_obs=20) -> dict
- run_regression_suite(panel_df, dependent_vars=None, model_specs=None) -> pandas.DataFrame

Default model specs:
Model A:
- y ~ sum_impact_score

Model B:
- y ~ sum_impact_score + mean_combined_confidence

Model C:
- y ~ sum_impact_score + qqq_return + soxx_return + smh_return

Model D:
- y ~ sum_impact_score + qqq_return + soxx_return + smh_return + nvda_return + tsm_adr_return

Model E:
- y ~ is_potentially_positive + is_potentially_negative + qqq_return + soxx_return + smh_return

Rules:
- Drop rows with NaN in required y/x columns for each model.
- If n < min_obs or n <= number_of_parameters + 2, do not fit; return status = insufficient_sample.
- Use HC1 robust standard errors if possible.
- Output:
    dependent_var
    model_name
    n_obs
    x_cols
    r_squared
    adj_r_squared
    coefficient for each x
    t-stat for each x
    p-value for each x if available
    status
    notes
- Do not interpret coefficients as causal.

4. Create src/backtest/regression_reporting.py

Generate Markdown report:
- Regression Diagnostics Summary
- Input Data
- Research Panel Construction
- Baseline Control Alignment
- Model Specifications
- Regression Results
- Insufficient Sample Warnings
- Interpretation Caveats
- Next Steps

Required caveats:
- This is not investment advice.
- Regression diagnostics are not causal proof.
- Small samples are not statistically reliable.
- Fixture or one-day samples cannot support inference.
- Out-of-sample validation is required.

5. Create scripts/build_research_panel.py

CLI args:
- --return-labels one or more CSV paths
- --prices optional normalized price CSV
- --output-panel optional
- --output-controls optional
- --label optional
- --aggregation-mode default target_day

Behavior:
- Load return labels.
- Build target-day panel.
- If prices provided, compute and align controls.
- Save panel.
- Save controls if computed.
- Print:
    input rows
    panel rows
    unique dates
    unique targets
    duplicate key count
    controls added
    output paths

6. Create scripts/run_regression_diagnostics.py

CLI args:
- --panel
- --output-csv optional
- --output-md optional
- --label optional
- --min-obs default 20

Behavior:
- Load panel.
- Run regression suite.
- Save CSV results.
- Save Markdown report.
- Print:
    panel rows
    dependent variables evaluated
    models evaluated
    fitted models
    insufficient-sample models
    output paths

7. Fixtures

Create data/fixtures/sample_research_panel_directional.csv or generate it in tests.

It should include at least:
- 30+ synthetic observations
- mixed positive, negative, neutral labels
- non-constant returns
- baseline controls
- enough rows so at least one regression can fit

Do not use this fixture to claim market evidence.

8. Tests

Create tests/test_research_panel.py:
- multiple return_labels files combine.
- target_day panel has one row per date/return_target.
- directional dummies are correct.
- unavailable return rows are excluded from regression panel by default.
- duplicate key detection works.

Create tests/test_baseline_controls.py:
- U.S. control returns computed correctly.
- Taiwan date aligns to previous available U.S. trading date.
- missing control ticker creates NaN column and warning rather than crash.
- no calendar-day assumption is made.

Create tests/test_regression_diagnostics.py:
- insufficient sample returns status insufficient_sample.
- synthetic 30+ row fixture fits at least one model.
- coefficients and t-stats columns are produced.
- NaN rows are dropped per model.
- no causal or trading language appears in report.

Create tests/test_research_panel_scripts.py:
- run build_research_panel.py using fixture data.
- run run_regression_diagnostics.py using synthetic panel.
- assert output CSV and Markdown files exist.
- assert required sections exist.
- assert caveats appear.

9. Update README.md

Add Phase 6C section:

Example fixture commands:
.\.venv\Scripts\python.exe scripts\build_research_panel.py --return-labels data\processed\return_labels_20260115.csv --prices data\processed\prices_fixture.csv --label fixture_20260115

.\.venv\Scripts\python.exe scripts\run_regression_diagnostics.py --panel data\processed\research_panel_fixture_20260115.csv --label fixture_20260115 --min-obs 20

Example multi-day command:
.\.venv\Scripts\python.exe scripts\build_research_panel.py --return-labels data\processed\return_labels_202606*.csv --prices data\processed\prices_live.csv --label live_archive

Clarify:
- Phase 6C requires many days for meaningful inference.
- One-day or fixture data only validates the machinery.
- In-sample / out-of-sample split will be Phase 6D or later.

10. Update .gitignore

Add generated outputs:
- data/processed/research_panel_*.csv
- data/processed/baseline_controls_*.csv
- data/processed/regression_results_*.csv
- data/reports/regression_diagnostics_*.md

Do not ignore synthetic fixtures.

Validation:
Run:
.\.venv\Scripts\python.exe -m pytest

Then run fixture workflow:
.\.venv\Scripts\python.exe scripts\build_research_panel.py --return-labels data\processed\return_labels_20260115.csv --prices data\processed\prices_fixture.csv --label fixture_20260115

.\.venv\Scripts\python.exe scripts\run_regression_diagnostics.py --panel data\processed\research_panel_fixture_20260115.csv --label fixture_20260115 --min-obs 20

Expected:
- With one-day fixture data, most real regressions should be insufficient_sample.
- With synthetic 30+ row fixture panel, tests should confirm regression machinery works.

Final response:
Summarize:
1. Files created or modified.
2. Tests run and results.
3. Research panel construction behavior.
4. Baseline control alignment behavior.
5. Regression output behavior.
6. Insufficient sample warnings.
7. Remaining limitations.
8. What remains for Phase 6D.

Do not implement Phase 6D.