Read AGENTS.md, README.md, and research_design.md first.

Task:
Implement Phase 5 only: deterministic Taiwan pre-market Markdown research report generator.

Important:
Do not implement LLM generation.
Do not call OpenAI or any external LLM API.
Do not collect live news.
Do not implement market data ingestion.
Do not implement backtesting.
Do not make trading recommendations.
Do not use words like buy, sell, guaranteed, will rise, will fall, target price, or must trade.

Context:
Phase 1 through Phase 4 are complete.
Current test suite passes.
Phase 3 produces:
data/processed/news_signals_YYYYMMDD.csv

Phase 4 produces:
data/processed/taiwan_impact_candidates_YYYYMMDD.csv

Now we need a deterministic Markdown report generator that summarizes:
1. overnight U.S. news themes
2. potentially affected Taiwan baskets / sectors / tickers
3. positive / negative / neutral candidate impacts
4. confidence and limitations
5. source provenance

Goal:
Generate:
data/reports/taiwan_premarket_report_YYYYMMDD.md

Inputs:
- data/processed/news_signals_YYYYMMDD.csv
- data/processed/taiwan_impact_candidates_YYYYMMDD.csv

Output:
- data/reports/taiwan_premarket_report_YYYYMMDD.md

Report title:
Taiwan Pre-Market Research Brief

Report structure:
1. Header
   - report date
   - generated timestamp
   - project name
   - research disclaimer

2. Executive Summary
   - number of raw/classified signals
   - number of mapped Taiwan impact candidates
   - top themes
   - dominant directional labels
   - short cautious summary

3. Overnight U.S. News Themes
   - grouped by sector and theme
   - include count of signals
   - include representative headlines
   - include sentiment labels

4. Taiwan Watchlist Candidates
   - grouped by taiwan_target_type:
       ticker
       basket
       proxy
       unmapped
   - include Taiwan target, sector, directional_impact_label, combined_confidence, impact_score
   - sort by absolute impact_score descending, then combined_confidence descending

5. Potentially Positive Candidates
   - list top candidates with cautious language

6. Potentially Negative Candidates
   - list top candidates with cautious language

7. Neutral / Unmapped / Requires Review
   - include neutral and unmapped candidates
   - explain that unmapped means no deterministic seed mapping was found

8. Source Provenance
   - list source, title, URL if available, published_at_utc
   - no full article body
   - no article scraping

9. Limitations
   - rule-based classifier
   - deterministic seed mapping only
   - no market validation yet
   - possible timestamp/source limitations
   - not investment advice

10. Next Research Step
   - mention that Phase 6 will add market data and backtest validation

Implementation requirements:

1. Create src/reporting/report_generator.py

Implement:
- load_report_inputs(signals_path, candidates_path) -> tuple[pandas.DataFrame, pandas.DataFrame]
- summarize_themes(signals_df) -> pandas.DataFrame or list[dict]
- summarize_candidates(candidates_df) -> pandas.DataFrame or list[dict]
- render_markdown_report(signals_df, candidates_df, report_date: str | None = None) -> str
- generate_report(signals_path, candidates_path, output_path, report_date: str | None = None) -> str

Rules:
- Deterministic output.
- No LLM.
- No live API calls.
- No trading recommendations.
- Use cautious language:
    potentially positive
    potentially negative
    neutral
    watchlist candidate
    requires validation
    deterministic mapping
    research signal
- Do not use forbidden language:
    buy
    sell
    guaranteed
    will rise
    will fall
    target price
    must trade

2. Create src/reporting/io.py

Implement:
- save_markdown_report(markdown_text: str, output_path)
- load_markdown_report(path)

3. Create scripts/generate_report.py

CLI arguments:
- --signals
- --candidates
- --output
- --date

Behavior:
- Load signals CSV.
- Load Taiwan impact candidates CSV.
- Generate Markdown report.
- Save to data/reports/taiwan_premarket_report_YYYYMMDD.md unless output is provided.
- Print summary:
    report date
    number of input signals
    number of impact candidates
    top 5 themes
    top 5 Taiwan targets
    output path

4. Tests

Create tests/test_report_generator.py:
- render_markdown_report returns a string.
- report contains required section headers:
    Executive Summary
    Overnight U.S. News Themes
    Taiwan Watchlist Candidates
    Potentially Positive Candidates
    Potentially Negative Candidates
    Neutral / Unmapped / Requires Review
    Source Provenance
    Limitations
- report contains non-investment-advice disclaimer.
- report does not contain forbidden terms:
    buy
    sell
    guaranteed
    will rise
    will fall
    target price
    must trade
- report includes at least one representative headline when signals are provided.
- report handles empty candidates gracefully.

Create tests/test_generate_report_script_fixture.py:
- Run fixture collection.
- Run rule-based classification.
- Run Taiwan mapping.
- Run report generation.
- Assert output Markdown exists.
- Assert required sections exist.
- Assert forbidden terms do not appear.
- Assert no full article body field appears.
- Assert report includes "requires validation" or similar caveat.

5. Update README.md

Add Phase 5 usage example:

PowerShell:
.\.venv\Scripts\python.exe scripts\collect_news.py --mode fixture --date 2026-01-15
.\.venv\Scripts\python.exe scripts\classify_news.py --input data\raw\news_20260115.csv --date 2026-01-15
.\.venv\Scripts\python.exe scripts\map_taiwan_impacts.py --input data\processed\news_signals_20260115.csv --date 2026-01-15
.\.venv\Scripts\python.exe scripts\generate_report.py --signals data\processed\news_signals_20260115.csv --candidates data\processed\taiwan_impact_candidates_20260115.csv --date 2026-01-15
.\.venv\Scripts\python.exe -m pytest

Clarify:
- Phase 5 is deterministic Markdown reporting.
- It is not an LLM-written report.
- It is not a trading recommendation.
- The report is a research briefing that requires market validation.

6. Update .gitignore if needed

Generated report files should not pollute source control:
- data/reports/taiwan_premarket_report_*.md

Optionally keep data/fixtures/sample_report.md only if created intentionally as fixture documentation.

Validation:
Run:
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe scripts\collect_news.py --mode fixture --date 2026-01-15
.\.venv\Scripts\python.exe scripts\classify_news.py --input data\raw\news_20260115.csv --date 2026-01-15
.\.venv\Scripts\python.exe scripts\map_taiwan_impacts.py --input data\processed\news_signals_20260115.csv --date 2026-01-15
.\.venv\Scripts\python.exe scripts\generate_report.py --signals data\processed\news_signals_20260115.csv --candidates data\processed\taiwan_impact_candidates_20260115.csv --date 2026-01-15
.\.venv\Scripts\python.exe -c "from src.reporting.report_generator import render_markdown_report; print('report import ok')"

If venv is unavailable, use the same Python executable used for Phase 4 tests and document the exact command used.

Final response:
Summarize:
1. Files created or modified.
2. Tests run and results.
3. Example command output.
4. Path to generated Markdown report.
5. Known limitations.
6. What remains for Phase 6.

Do not implement Phase 6.